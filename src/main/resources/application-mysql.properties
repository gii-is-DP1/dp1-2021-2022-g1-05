# database init, supports mysql too
database=mysql
spring.datasource.url=jdbc:mysql://localhost/flippingboards
spring.datasource.username=flippingboards
spring.datasource.password=flippingboards
# SQL is written to be idempotent so this is safe
spring.datasource.initialization-mode=always
